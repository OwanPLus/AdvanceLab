const int groundToground = 8;
const int unfiltered = A0;
const int PWNPin = 3;
const int filtered = A2;

void setup() {
  Serial.begin(115200);
  tone(groundToground, 60);
  tone(PWNPin, 60);
}

void loop() {
  unsigned long t = millis();
  int gtg = analogRead(A0);
  int f = analogRead(A2);

  Serial.print(t);
  Serial.print(',');
  Serial.print(gtg);
  Serial.print(',');
  Serial.println(f);
  
  delay(1);
}
